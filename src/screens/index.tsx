import { View } from "react-native";
import { Button } from "tamagui";

const Home = () => {
  return (
    <View className="">
      <Button> Hello</Button>
    </View>
  );
};

export default Home;
